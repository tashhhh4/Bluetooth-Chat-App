[project]
name = "Blu2"
version = "0.0.0"
description = "Communicate with Android devices over a Bluetooth connection."
readme = "README.md"
requires-python = ">=3.11,<3.12"
dependencies = [
    "buildozer>=1.6.0",
    "kivy>=2.3.1",
    "kivymd>=1.2.0",
    "sqlalchemy>=2.0.51",
]
